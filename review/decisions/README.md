# 可変盤面レビュー判断

`variable-stage-review-round1.json`は、可変エリア108問候補プールに対する承認済みの採否判断です。

完成バンク生成では次を厳守します。

- `status: keep`の84問だけを採用する
- `status: reject`の24問を含めない
- 未判断や保留を自動採用しない
- JSON内容のSHA-256を完成バンクmanifestへ記録する
- 判断JSONが変わった場合は完成バンクを再生成し、差分をレビューする

この判断ファイル自体はゲーム実行時に読み込みません。ランダム練習への接続は別PR・別承認です。
